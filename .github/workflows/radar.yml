- name: Node.js'i Kur
        uses: actions/setup-node@v4
        with:
          node-version: '18'

      - name: Botu Ateşle
        env:
          LWATLAS_API_KEY: ${{ secrets.LWATLAS_API_KEY }}
        run: node bot.js
